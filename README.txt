To run the program, on the terminal please do the following commands:
-  npm install
-  npm run dev
-  ctrl+click on the local link it provides on the terminal to be redirected to 
  the website on the browser
-  test out and try the website functionalities